# Syncorda brand record

## Name

**Syncorda** is the project’s open-source name. The GitHub repository is `tommyyzhao/syncorda`.

This record is project-maintenance documentation, not legal advice. The project does not make trademark claims or require a trademark-registration process for open-source development.

## Visual mark

`Resources/Brand/app-mark.png` is the selected project mark: one local audio waveform branches to two speaker endpoints. It is deliberately wordless. `Resources/AppIcon.icns` packages it for macOS.

## Identifiers

The app bundle is `Syncorda.app`, the executable is `SyncordaApp`, the CLI is `syncordactl`, the bundle identifier is `io.github.tommyyzhao.syncorda`, the local socket is `/tmp/syncorda-<uid>.sock`, and profiles live at `~/Library/Application Support/Syncorda/profiles.json`.
